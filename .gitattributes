# Enforce LF (Unix) line endings across platforms to keep Spotless and CI consistent
# Developers on Windows should set: git config core.autocrlf false (or use .editorconfig) and rely on IDE to display endings.
* text=auto eol=lf

# Explicit common text types (redundant but keeps intent clear)
*.java text eol=lf
*.xml text eol=lf
*.properties text eol=lf
*.json text eol=lf
*.md text eol=lf
*.txt text eol=lf

# Binary files
*.jar binary
*.war binary
*.zip binary
*.tar binary
*.gz binary
*.class binary