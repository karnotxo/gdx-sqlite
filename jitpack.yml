jdk:
  - openjdk17

install:
  - ./gradlew clean :gdx-sqlite:build :gdx-sqlite-desktop:build :gdx-sqlite-android:build :gdx-sqlite-robovm:build -x test

# Use Gradle for building (updated for JitPack)